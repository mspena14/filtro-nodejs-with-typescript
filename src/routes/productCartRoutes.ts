import { Router } from 'express';
import { ProductCartController } from '../controllers';

export const productCartRouter = Router();

// productCartRouter.post('/', ProductCartController.createProductCart)
// productCartRouter.delete('/:id/Products/:productId', ProductCartController.deleteProductCartProducts)
// productCartRouter.patch('/:id/Products/:productId', ProductCartController.updateProductCartProductsQty)